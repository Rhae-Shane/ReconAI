# Optional for harness CLI only. Core tests/batch work with empty env.
# Redis / Supabase / Razorpay are used by the Next.js app, not this package.
# Copy from ../app/.env.local if you want local OPENAI for residual judge experiments.

OPENAI_API_KEY=
# CHATGPT_API_KEY=
